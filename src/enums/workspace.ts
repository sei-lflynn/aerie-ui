export enum WorkspaceContentType {
  Binary = 'BINARY',
  Directory = 'DIRECTORY',
  Json = 'JSON',
  Metadata = 'METADATA',
  Sequence = 'SEQUENCE',
  Text = 'TEXT',
  Unknown = 'UNKNOWN',
  Workspace = 'WORKSPACE', // not a real value from the service
}
